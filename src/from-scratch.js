const makeCounter = () => {
};

function Circle() {
}

Circle.prototype.getArea = function () {
};

Circle.prototype.getCircumference = function () {
};

// Do the other Circle methods following this pattern!

function Teacher() {
}

// The Teacher methods should use the same approach as Circle!

module.exports = {
  makeCounter,
  Circle,
  Teacher,
};
